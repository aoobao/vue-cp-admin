export default {
  size: 'medium',
  // vue-ls options
  storageOptions: {
    namespace: 'cp__', // key prefix
    name: 'ls', // name variable Vue.[ls] or this.[$ls],
    storage: 'local', // storage name session, local, memory
  },
  defaultImage: 'images/picture.png'
}
